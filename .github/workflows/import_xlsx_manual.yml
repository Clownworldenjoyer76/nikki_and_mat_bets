name: Import XLSX Manual

on:
  workflow_dispatch:
    inputs:
      folder:
        required: true
        type: string
      week:
        required: true
        type: string
      season:
        required: true
        type: string

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: |
          python -m pip install openpyxl
          python scripts/xlsx_to_csv.py "${{ inputs.folder }}" "${{ inputs.week }}" "${{ inputs.season }}"
